import PropTypes from 'prop-types';

const ChartTooltipPropType = {
  description: PropTypes.string,
  labelFormatter: PropTypes.func,
  valueFormatter: PropTypes.func,
};

export default ChartTooltipPropType;
